# Web Search Module Dependencies

# Core dependencies
requests>=2.31.0
redis>=5.0.0
pydantic>=2.0.0
pyyaml>=6.0.0

# Optional: DuckDuckGo search (recommended for fallback)
duckduckgo-search>=4.0.0

# For HTML parsing (if using DuckDuckGo HTML interface)
beautifulsoup4>=4.12.0

# Already included in main SAP_LLM requirements
# pydantic-settings>=2.0.0
